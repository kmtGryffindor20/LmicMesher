; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html



[board-heltec]
board = heltec_wifi_lora_32_V3
cs = 8
rst = 12
irq = 14
io1 = 13

[board-ttgo-beam]
board = ttgo-t-beam
cs = 18
rst = 23
irq = 26
io1 = 33

lib_deps = 
    ; bogde/HX711@^0.7.5
    ; sparkfun/SparkFun APDS9960 RGB and Gesture Sensor@^1.4.2
    ; adafruit/Adafruit BME680 Library@^2.0.4
    ; adafruit/Adafruit Unified Sensor@^1.1.14
    ; mcci-catena/MCCI LoRaWAN LMIC library@^4.1.1
	; lewisxhe/XPowersLib@^0.2.4
	; adafruit/Adafruit BME680 Library
	; adafruit/Adafruit Unified Sensor

; [env:heltec_wifi_lora_32_V3-AE14]
; monitor_port = /dev/ttyUSB0
; upload_port = /dev/ttyUSB0
; platform = espressif32
; board = heltec_wifi_lora_32_V3
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-heltec.CS}
; 	-D RST=${board-heltec.RST}
; 	-D IRQ=${board-heltec.IRQ}
; 	-D IO1=${board-heltec.IO1}
; 	; -D SHOULD_USE_LMIC=1
; 	; -D IS_GATEWAY=1
; 	-D HELTEC=1


; [env:heltec_wifi_lora_32_V3_2-DE6C]
; monitor_port = /dev/ttyUSB6
; upload_port = /dev/ttyUSB6
; platform = espressif32
; board = heltec_wifi_lora_32_V3
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-heltec.CS}
; [env:ttgo-t-beam2-1850]
; monitor_port = /dev/ttyACM0
; upload_port = /dev/ttyACM0
; platform = espressif32
; board = ttgo-t-beam
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-ttgo-beam.CS}
; 	-D RST=${board-ttgo-beam.RST}
; 	-D IRQ=${board-ttgo-beam.IRQ}
; 	-D IO1=${board-ttgo-beam.IO1}

; 	-D RST=${board-heltec.RST}
; 	-D IRQ=${board-heltec.IRQ}
; 	-D IO1=${board-heltec.IO1}
; 	-D HELTEC=1

; [env:heltec_wifi_lora_32_V3_2-DE6C_]
; monitor_port = /dev/ttyUSB2
; upload_port = /dev/ttyUSB2
; platform = espressif32
; board = heltec_wifi_lora_32_V3
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-heltec.CS}
; 	-D RST=${board-heltec.RST}
; 	-D IRQ=${board-heltec.IRQ}
; 	-D IO1=${board-heltec.IO1}
; 	-D HELTEC=1



; [env:ttgo-t-beam-121C]
; monitor_port = /dev/ttyACM0
; upload_port = /dev/ttyACM0
; platform = espressif32
; board = ttgo-t-beam
; framework = arduino
; monitor_speed = 115200
; [env:ttgo-t-beam2-1850]
; monitor_port = /dev/ttyACM0
; upload_port = /dev/ttyACM0
; platform = espressif32
; board = ttgo-t-beam
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-ttgo-beam.CS}
; 	-D RST=${board-ttgo-beam.RST}
; 	-D IRQ=${board-ttgo-beam.IRQ}
; 	-D IO1=${board-ttgo-beam.IO1}

; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-ttgo-beam.CS}
; 	-D RST=${board-ttgo-beam.RST}
; 	-D IRQ=${board-ttgo-beam.IRQ}
; 	-D IO1=${board-ttgo-beam.IO1}


; [env:ttgo-t-beam2-1850]
; monitor_port = /dev/ttyACM1
; upload_port = /dev/ttyACM1
; platform = espressif32
; board = ttgo-t-beam
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-ttgo-beam.CS}
; 	-D RST=${board-ttgo-beam.RST}
; 	-D IRQ=${board-ttgo-beam.IRQ}
; 	-D IO1=${board-ttgo-beam.IO1}
; 	-D SHOULD_USE_LMIC=1
; 	-D Lilygo=1
;     -D IS_GATEWAY=1


; [env:ttgo-t-beam2-185]
; monitor_port = /dev/ttyACM1
; upload_port = /dev/ttyACM1
; platform = espressif32
; board = ttgo-t-beam
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release
; build_flags = 
; 	-D CORE_DEBUG_LEVEL=5
; 	-D CS=${board-ttgo-beam.CS}
; 	-D RST=${board-ttgo-beam.RST}
; 	-D IRQ=${board-ttgo-beam.IRQ}
; 	-D IO1=${board-ttgo-beam.IO1}



; ; --- ENVIRONMENT 1: GATEWAY NODE (Mesh RX -> LoRaWAN TX) ---
; [env:ttgo_gateway]
; upload_port = /dev/ttyACM0
; platform = espressif32
; board = ttgo-t-beam
; framework = arduino
; monitor_speed = 115200
; lib_ldf_mode = deep+
; build_type = release

; ; Ensure the MCCI LMIC library is included for LoRaWAN
; lib_deps = 
;     ; https://github.com/lewisxhe/LM-SE-LoRaMesher.git#development
;     ; mcci-catena/MCCI LoRaWAN LMIC library@^4.1.1
;     ; Add any other shared libraries here

; build_flags = 
;     -D CORE_DEBUG_LEVEL=5
;     -D CS=${board-ttgo-beam.CS}
;     -D RST=${board-ttgo-beam.RST}
;     -D IRQ=${board-ttgo-beam.IRQ}
;     -D IO1=${board-ttgo-beam.IO1}
;     ; CRITICAL: Define IS_GATEWAY to enable LoRaWAN forwarding logic
;     -D IS_GATEWAY=1
;     ; Add region definition for LMIC (e.g., EU868, US915, etc.)
;     -D CFG_in866=1
;     ; Set LMIC version if needed
;     ; -D LMIC_LORAWAN_V102



[env:ttgo-t-beam2-1850]
monitor_port = /dev/ttyACM1
upload_port = /dev/ttyACM1
platform = espressif32
board = ttgo-t-beam
framework = arduino
monitor_speed = 115200
lib_ldf_mode = deep+
build_type = release
build_flags = 
	-D CORE_DEBUG_LEVEL=5
	-D CS=${board-ttgo-beam.CS}
	-D RST=${board-ttgo-beam.RST}
	-D IRQ=${board-ttgo-beam.IRQ}
	-D IO1=${board-ttgo-beam.IO1}
	; -D IS_GATEWAY=1
	-D CFG_in866=1
	-D REBROADCAST_HELLO_ON_ROUTE_CHANGE=1

